# ZhuoDazi Android UI

Flutter module embedded by the native Android host in `../android`. The native
host keeps ownership of overlay windows, foreground services, local settings,
licensing, companion networking, and GIF import.

Run `flutter pub get`, `flutter analyze`, and `flutter test` from this directory
before building the Android host.
